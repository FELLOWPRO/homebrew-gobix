# Homebrew Tap for Gobix

Install the Gobix CLI on macOS:

```bash
brew install fellowpro/gobix/gobix-cli
```

After installation, configure access with a Gobix API key:

```bash
gobix configure --base-url https://dev.gobix.de --api-key gobix_v1_...
gobix self-test
```
